<% @articles.each do |article| %>
  <li>
    <%= link_to article.title, article_path(article.id) %>
    <br>
    <%= article.content.truncate(50,separator: /[\.\,:]/, omission: ' ...') %>
  </li>
<% end %>
